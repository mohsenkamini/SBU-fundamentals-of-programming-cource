#include <iostream>
using namespace std;

int main () {
    int size;
    cin >> size;
    int* pointer;
    pointer = new type [size];

    delete [] pointer; // free the memory

    int* p = new int; Ss
    delete p;
}
